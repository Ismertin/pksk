import { useEffect } from "react";
import { useNavigate } from "react-router-dom";

export const useClickService = {
    useEffect(() =>{
        const handleClick = (event) => {
            
        }
        const nav = useNavigate()
    })

    }
};
